// import { SyntheticEvent, useState } from "react";
// import { useEffect } from "react"

// const Home = (props: { firstName: string }) => {
const Home = (props) => {
    return (
        <div>
            {/* {props.firstName ? 'Hi' + props.firstName : 'You are not logged in'} */}
        </div>
    )
}

export default Home